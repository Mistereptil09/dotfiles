{{- $osid := .chezmoi.os -}}
{{- if hasKey .chezmoi.osRelease "id" -}}
{{-   $osid = .chezmoi.osRelease.id -}}
{{- end -}}

[data]
    os = {{ $osid | quote }}
    hostname = {{ .chezmoi.hostname | quote }}
